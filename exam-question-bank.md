---
name: exam-question-bank
description: "从课程考点笔记出发，构建结构化题库 Markdown + 交互式 HTML 题库页面（答案可折叠、按考点组织、含教材练习题）。与 exam-review skill 配套使用。Use when user says: 生成题库、出题、练习题、刷题页面、题目HTML、question bank"
argument-hint: [考点笔记md路径 或 考点描述]
allowed-tools: Bash(*), Read, Write, Edit, Grep, Glob, Agent
---

# Exam Question Bank: 考点笔记 → 结构化题库 + HTML 刷题页

与 `exam-review` skill 配套：exam-review 生成考点笔记 HTML，本 skill 生成独立的**题库 HTML**。两个页面并列，笔记页看知识点，题库页刷题查漏补缺。

**Inputs:**
- `$ARGUMENTS` — 已有考点笔记 Markdown 路径（如 `复习笔记_考点版.md`），或考点列表描述
- 可选：教材 PDF 路径（用于抽取教材原题）

---

## 设计原则

1. **题库独立**：不把题目并入考点笔记，单独维护 `期末题库.md`，便于扩充和搜索语义清晰。
2. **按考点组织**：`考点N：名称 → 题1/题2/...`，每题有题型标签、答案、对应教材页码。
3. **覆盖多种考法**：每个考点尽量覆盖选择/填空/简答/推导/代码阅读/综合分析中的若干种。
4. **答案折叠**：HTML 页面中答案默认折叠，点击展开，适合做题后自查。
5. **教材练习题**：从 PDF 各章节练习页抽取高价值题，标注 PDF 页码，形成"题→教材"闭环。

---

## Workflow

### Step 1：读取考点列表

从已有考点笔记提取所有考点：

```bash
grep "^### 考点" 复习笔记_考点版.md
```

或直接让用户提供考点大纲。

### Step 2：设计题库 Markdown 结构

`期末题库.md` 的统一格式：

```markdown
# 课程名称期末题库（全覆盖结构化版）

> 目标：知识点全覆盖 + 考法全覆盖
> 范围依据：`复习笔记_考点版.md`

---

## 一、考点-考法总览

| 考点 | 高频考法 | 备注 |
| --- | --- | --- |
| 考点1 XXX | 选择、填空、推导 | 核心公式常考 |
| 考点2 XXX | 简答、代码阅读 | ... |

---

## 二、按考点题库

## 第一部分：XXX基础

### 考点1：XXX
- **覆盖考法：** 选择 / 填空 / 简答 / 代码

#### 题1【选择】
题干...
A. ...  B. ...  C. ...  D. ...

- **答案：** B
- **对应教材：** §3.1 p85-104

#### 题2【填空】
...填空题干...

- **答案：** ...
- **对应教材：** §3.1 p85

#### 题3【简答】
...简答题干...

- **答案要点：**
  1. 第一点
  2. 第二点
- **对应教材：** §3.2 p90-95

#### 题4【教材练习】
...从教材练习页抽取的题目...（教材 pXX）

- **答案要点：**
  - 要点1
  - 要点2
```

**关键格式约束**（影响 HTML 折叠逻辑，必须严格遵守）：
- 每道题用 `####` 四级标题（`#### 题N【题型】`）
- 答案块**必须**以 `- **答案：**`、`- **答案要点：**` 或 `- **参考答案：**` 开头
- 答案块内容直接跟在上面三个触发词后面，用列表或代码块
- **不要**在题库 Markdown 里用 `<details>` HTML 标签（生成器用 JS 动态包裹，原生 HTML 会破坏结构）
- `---` 分隔线用于分隔考点，是答案块的终止标志之一
- 不要在 LaTeX 中用 `|`（破坏表格），改用 `\lVert`

### Step 3：生成题库内容

按考点逐一生成题目，每个考点至少覆盖 3 种考法。题目质量原则：
- **选择题**：设计合理干扰项（常见误解作为选项）
- **填空题**：考核核心公式/定义
- **简答题**：答案写"评分点式"（编号列表），不写大段散文
- **综合/代码题**：基于具体实验/训练场景，考代码阅读、结果分析、方案设计（参考 memory 笔记）
- **教材练习**：来自教材各节练习页，标注页码

### Step 4：从 PDF 抽取教材练习题（可选但推荐）

每次读取不超过 6 页（工具限制），按章节练习页分批读取：

```
# 读取章节练习页（每次 ≤6 页）
Read PDF pages X-Y  →  识别"练习"小节  →  抽取高价值题目
```

**页码定位策略**：
- 各章节练习通常在章节末尾（小结之后）
- 先读预估范围的前几页确认位置，再精确读取

抽取后按以下格式插入对应考点末尾：

```markdown
#### 题N【教材练习】
原题题干（教材 pXX）

- **答案要点：**
  - 要点1（来自教材解析或推导）
  - 要点2
```

### Step 5：配置并运行生成器

`generate_review_page.py` 支持 `--layout standalone` 模式，生成不带左侧 PDF 面板的单栏题库页：

```bash
python3 generate_review_page.py \
  --input  期末题库.md \
  --output 期末题库.html \
  --title  "课程名称期末题库" \
  --layout standalone
```

**`standalone` 模式说明**：
- 不显示左侧 PDF 面板
- 右侧题库内容全宽展示
- 答案默认折叠（`点击展开答案`），点击展开
- 支持顶部搜索框实时高亮

### Step 6：启动服务器并访问

```bash
cd WORK_DIR
python3 -m http.server 8080
```

访问 `http://localhost:8080/期末题库.html`

---

## generate_review_page.py 的答案折叠机制（重要）

HTML 中答案折叠由 JS `wrapAnswers()` 函数实现，理解机制才能写出正确格式：

```
对每个 h4 标题：
  向后扫描兄弟元素，直到遇到下一个 h2/h3/h4/hr 为止
  在这段范围内找第一个"答案触发元素"
  把从触发元素到范围末尾的所有元素包进 <details class="answer-block">
```

**答案触发条件**（`isAnswerStart` 函数）：元素 `textContent` 以下列之一开头：
- `答案：`
- `答案要点：`
- `参考答案：`

**常见坑**：
1. `<details>` HTML 标签不要手写进 Markdown — JS 会动态创建，手写的会破坏 DOM 结构
2. 答案触发词前必须是 `- **...**` 格式的列表项，使其成为独立的 `<li>` 元素
3. 若题目中有代码块在答案前，代码块不会触发折叠，正常

---

## generate_review_page.py 修改记录（本项目已做的关键修改）

如果从其他项目复制 `generate_review_page.py`，需要确认以下两处已修改：

**修改点1：`wrapAnswers()` 遍历 `h4` 而非 `h3`**

原始代码遍历 `h3`，但题目是 `h4` 级别，导致折叠完全不工作。需改为：

```javascript
// 原来（错误）：
var headings = content.querySelectorAll('h3');

// 修改后（正确）：
var headings = content.querySelectorAll('h4');
```

**修改点2：两处 `while` 循环都加 `H4` 终止条件**

```javascript
// 原来（错误）：两处 while 都只有 H2/H3/HR
while (current && current.tagName !== 'H2' && current.tagName !== 'H3' && current.tagName !== 'HR')

// 修改后（正确）：加入 H4
while (current && current.tagName !== 'H2' && current.tagName !== 'H3' && current.tagName !== 'H4' && current.tagName !== 'HR')
```

不加 `H4`：答案块会吞掉后面所有题目直到下一个 `###` 考点标题。  
只加终止不改遍历对象：题目都展示了但没有折叠。  
两处都修改才能正常工作。

---

## 文件组织

```
WORK_DIR/
├── 复习笔记_考点版.md        # 考点笔记（exam-review skill 生成）
├── 复习笔记_考点版.html      # 双栏笔记页（左PDF+右笔记）
├── 期末题库.md               # 题库（本 skill 生成）
├── 期末题库.html             # 单栏题库页（本 skill 生成）
└── generate_review_page.py  # 共用生成器
```

两个 HTML 页面可互相加入口链接（在各自 Markdown 顶部加 `[→ 题库页](期末题库.html)`）。

---

## 扩充题库的建议流程

1. **补充教材真题**：读 PDF 各章节练习页，抽取高价值题加入对应考点末尾
2. **按题型扩充**：若某考点缺少代码题，补充代码阅读题
3. **加入历年真题**：单独加一节 `## 三、历年真题` 或混入各考点
4. **派生无答案版**：用脚本去掉答案块，生成考前自测版

重新生成 HTML 只需重跑：
```bash
python3 generate_review_page.py --input 期末题库.md --output 期末题库.html --title "课程名称期末题库" --layout standalone
```
