# 实验2：MLP 与激活函数挑战

## 一、实验目标

通过本实验，学生需要掌握：

1.  理解 **多层感知机（MLP）** 的结构与作用
2.  理解 **非线性激活函数的必要性**
3.  对比不同激活函数对模型训练的影响
4.  能够绘制并分析 **Loss收敛曲线**
5.  建立“实验对比”的基本方法

## 二、实验背景

在前两个实验中：

- 线性回归 → 只能拟合线性关系
- Softmax回归 → 只能做线性分类

👉 本实验引入 **MLP（多层感知机）**

MLP结构：

关键点：

👉 **激活函数 = 引入非线性能力**

没有激活函数：

线性 + 线性 = 线性（没有提升）

## 三、实验任务任务1：实现一个MLP模型

模型结构：

- 输入层：784（28×28图像展开）
- 隐藏层：256
- 输出层：10

即：

要求：

- 手动定义参数（W1, b1, W2, b2）
- 不使用 nn.Sequential 封装整个模型（允许使用基础tensor）

## 任务2：实现不同激活函数

至少实现以下3种：

### 1 ReLU

### 2 Sigmoid

### 3 Tanh

## 任务3：训练模型并记录Loss

要求：

- 每种激活函数训练一次
- 每次训练至少 **5个epoch**
- 记录每个epoch的：
    - train loss
    - train acc
    - test acc

## 任务4：绘制Loss曲线

要求：

- 在同一张图上画出：
    - ReLU曲线
    - Sigmoid曲线
    - Tanh曲线

横轴：epoch  
纵轴：loss

## 任务5：对比分析

重点分析：

1.  哪个激活函数收敛最快？
2.  哪个最稳定？
3.  是否存在“训练很慢”的情况？
4.  为什么会这样？

## 四、实验提示（非常关键）

### 1 梯度消失问题

Sigmoid / Tanh 可能出现：

梯度越来越小 → 学不动

### 2 ReLU优势

ReLU通常：

- 收敛更快
- 更稳定

### 3 参数初始化建议

使用：

Python运行torch.normal(0, 0.01)

### 4 输入必须reshape

Python运行X = X.reshape((-1, 784))

## 五、实验报告要求

### 1 实验目的

说明：

- 为什么需要MLP
- 为什么需要激活函数

### 2 实验原理

包括：

- MLP结构
- 激活函数作用
- ReLU / Sigmoid / Tanh 区别

### 3 实验过程

说明：

- 模型实现
- 激活函数实现
- 训练流程

### 4 实验结果

必须包含：

- 三种激活函数的loss曲线
- 准确率对比

### 5 实验分析

重点回答：

- 为什么ReLU更快？
- 为什么Sigmoid慢？
- 激活函数如何影响梯度传播？

### 6 实验总结

总结本实验收获。

## 六、评分标准

| 项目  | 分值  |
| --- | --- |
| MLP实现 | 20  |
| 激活函数实现 | 20  |
| 训练结果 | 20  |
| 曲线绘制 | 15  |
| 分析深度 | 15  |
| 报告规范 | 10  |