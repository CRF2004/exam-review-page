# 实验报告

**课程名称：** 深度学习  
**实验名称：** 实验2：MLP 与激活函数挑战  
**实验日期：** 2026/03/20  
**实验地点：** B10楼 303房  

## 1 实验目的

1. 理解多层感知机（MLP）的基本结构，明确隐藏层如何提升模型的表达能力。
2. 理解非线性激活函数的必要性，掌握“线性层 + 激活函数”构成非线性映射的原因。
3. 学会手动定义 MLP 参数，并使用基础张量运算完成前向传播、损失计算、反向传播和参数更新。
4. 通过对比 ReLU、Sigmoid 和 Tanh 三种激活函数的训练过程，分析不同激活函数对收敛速度和分类性能的影响。
5. 学会记录 train loss、train acc、test acc，并结合 loss 曲线进行实验分析。

## 2 实验原理

### 2.1 MLP 模型原理

单层线性模型只能学习线性可分关系，而多层感知机通过在输入层和输出层之间加入隐藏层，可以学习更复杂的非线性特征。本实验采用的网络结构为：

- 输入层：784
- 隐藏层：256
- 输出层：10

前向传播过程如下：

`H = activation(XW1 + b1)`

`Y_hat = HW2 + b2`

其中，`activation` 表示激活函数，`W1、b1、W2、b2` 为手动定义的可训练参数。

### 2.2 激活函数的作用

如果隐藏层后面不加入激活函数，那么多层线性变换的结果仍然等价于一次线性变换，模型表达能力不会真正增强。因此，激活函数的作用是为网络引入非线性能力。

本实验比较了三种常见激活函数：

1. **ReLU**：`f(x) = max(0, x)`  
   计算简单，正半轴梯度恒为 1，通常收敛较快。

2. **Sigmoid**：`f(x) = 1 / (1 + e^(-x))`  
   输出范围在 `(0, 1)`，当输入绝对值较大时容易进入饱和区，梯度变小，训练速度可能变慢。

3. **Tanh**：`f(x) = tanh(x)`  
   输出范围在 `(-1, 1)`，相较于 Sigmoid 以 0 为中心，梯度传播更平衡，但仍有饱和风险。

### 2.3 损失函数与优化方法

本实验采用交叉熵损失函数进行 10 分类训练，并使用小批量随机梯度下降（Mini-batch SGD）更新参数。参数初始化按照实验要求使用 `torch.normal(0, 0.01)` 完成。

## 3 实验步骤

### 3.1 数据准备

本实验环境已安装 `torchvision`，因此直接使用 `torchvision.datasets.FashionMNIST` 下载并读取 Fashion-MNIST 数据集。该数据集包含：

- 训练集：60000 张灰度图像
- 测试集：10000 张灰度图像
- 图像尺寸：`28x28`
- 类别数：10

图像展平后可直接得到 `784` 维输入，满足实验要求。

### 3.2 模型实现

手动定义参数：

- `W1`：形状 `(784, 256)`
- `b1`：形状 `(256,)`
- `W2`：形状 `(256, 10)`
- `b2`：形状 `(10,)`

然后使用基础张量运算完成前向传播，不使用 `nn.Sequential`、`nn.Linear` 或 `torch.optim` 等高级封装。

### 3.3 激活函数实现

分别实现了：

- ReLU
- Sigmoid
- Tanh

训练时仅替换隐藏层激活函数，其余训练配置保持一致，从而保证对比公平。

### 3.4 训练设置

- Epochs：10
- Batch size：256
- Learning rate：0.2

每个 epoch 记录：

- train loss
- train acc
- test acc

同时将三种激活函数的训练损失曲线绘制在同一张图中进行对比。

## 4 实验结果

### 4.1 各激活函数最终结果对比

| 激活函数 | 最终 Train Loss | 最终 Train Acc | 最终 Test Acc | 最佳 Test Acc |
| --- | ---: | ---: | ---: | ---: |
| ReLU | 0.3387 | 0.8778 | 0.8314 | 0.8499 |
| Tanh | 0.3714 | 0.8641 | 0.8300 | 0.8369 |
| Sigmoid | 0.4433 | 0.8425 | 0.8245 | 0.8245 |

从结果可以看出，三种激活函数都能够完成 Fashion-MNIST 分类任务，但性能存在明显差异。`ReLU` 的最佳测试准确率最高，`Sigmoid` 的整体表现最弱。

### 4.2 各激活函数训练过程对比

| 激活函数 | Epoch 1 Test Acc | Epoch 5 Test Acc | Epoch 10 Test Acc |
| --- | ---: | ---: | ---: |
| ReLU | 0.7509 | 0.7950 | 0.8314 |
| Sigmoid | 0.6636 | 0.7805 | 0.8245 |
| Tanh | 0.7509 | 0.7915 | 0.8300 |

进一步观察最佳测试准确率出现的位置：

| 激活函数 | 最佳 Test Acc | 对应 Epoch |
| --- | ---: | ---: |
| ReLU | 0.8499 | 6 |
| Tanh | 0.8369 | 6 |
| Sigmoid | 0.8245 | 10 |

可以看到：

- `ReLU` 和 `Tanh` 在前期收敛更快，第 1 个 epoch 测试准确率已经达到 `0.7509`。
- `Sigmoid` 第 1 个 epoch 仅为 `0.6636`，说明早期学习速度明显偏慢。
- `ReLU` 在第 6 个 epoch 达到全实验最高的 `0.8499`，峰值表现最好。
- `Sigmoid` 直到第 10 个 epoch 才达到自己的最好结果，说明其收敛速度最慢。

### 4.3 Loss 曲线

![MLP Loss Curve](exp2_loss_curve.png)

从损失曲线可以看到：

- `ReLU` 的 loss 下降速度最快，前几轮下降最明显；
- `Tanh` 的整体走势与 `ReLU` 接近，但曲线略高；
- `Sigmoid` 的 loss 始终高于前两者，说明优化效率较低。

## 5 实验分析

### 5.1 哪个激活函数收敛最快

从本次实验结果看，`ReLU` 与 `Tanh` 在前 1 个 epoch 的测试准确率相同，前期收敛都比较快；但结合 loss 曲线和后续峰值表现，`ReLU` 的整体收敛效率更高，因为它在第 6 个 epoch 达到全实验最高的 `0.8499`，并且 loss 下降更快。

### 5.2 哪个最稳定

`Tanh` 与 `ReLU` 都较稳定，且整体精度明显高于 `Sigmoid`。其中 `Tanh` 的最终结果与 `ReLU` 接近，波动也较小；`ReLU` 的峰值更高，但测试准确率会有一定波动。`Sigmoid` 虽然也能逐步提升，但整个过程更慢、loss 更高。

### 5.3 是否存在训练很慢的情况

存在，`Sigmoid` 就表现出明显的训练偏慢现象。它在第 1 个 epoch 的测试准确率只有 `0.6636`，明显低于 `ReLU` 和 `Tanh` 的 `0.7509`；直到第 10 个 epoch 才达到自己的最好结果 `0.8245`。

### 5.4 为什么会这样

主要原因在于梯度传播性质不同：

1. **Sigmoid 容易进入饱和区**  
   当输入较大或较小时，Sigmoid 输出接近 1 或 0，此时导数接近 0，反向传播时梯度会迅速变小，因此参数更新速度较慢。

2. **Tanh 以 0 为中心**  
   相比 Sigmoid，Tanh 的输出更对称，优化时梯度方向更平衡，所以训练速度通常会优于 Sigmoid。

3. **ReLU 在正半轴梯度恒定**  
   ReLU 在正区间不会明显饱和，因此反向传播效率更高，训练过程通常更快，峰值效果也更好。

### 5.5 对实验结果的理解

本次实验说明，激活函数不仅影响最终精度，更影响训练过程本身。即使在相同网络结构、相同初始化方式和相同学习率下，不同激活函数仍会产生不同的 loss 曲线和准确率表现。Fashion-MNIST 上三种激活函数都能学习到有效特征，但 `ReLU` 的综合表现最好，`Sigmoid` 最慢，这与理论分析基本一致。

## 6 实验总结

本实验基于 Fashion-MNIST 数据集，手动实现了一个 `784-256-10` 的多层感知机，并完成了三种激活函数的对比训练。通过实验，我进一步理解了以下几点：

1. MLP 通过隐藏层与激活函数获得了线性模型不具备的非线性建模能力。
2. 激活函数会直接影响梯度传播效率，从而影响收敛速度和最终性能。
3. Sigmoid 更容易出现梯度变小的问题，因此训练速度通常偏慢。
4. ReLU 和 Tanh 都能较快完成当前分类任务，其中 ReLU 的最佳测试准确率最高。
5. 通过记录 loss、train acc 和 test acc，并绘制对比曲线，可以更直观地分析模型训练过程。

综上，本实验不仅完成了 MLP 的手动实现，也验证了不同激活函数对训练效率和分类性能的直接影响，为后续学习更深层神经网络打下了基础。
