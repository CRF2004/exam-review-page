## 实验名称

**BatchNorm 对 ResNet-18 训练稳定性的影响：Loss 波动分析与 Warmup 修复实验**

## 一、实验目的

通过本实验，你将完成以下学习目标：

1.  理解 **BatchNorm（批量归一化）** 在深层网络训练中的作用。
2.  观察 **加入 BatchNorm 前后，ResNet-18 的 Loss 曲线差异**。
3.  学会识别 **不稳定训练轨迹** 的典型表现。
4.  理解 **Warmup（学习率预热）** 如何缓解训练初期震荡。
5.  能够从实验结果中总结：
    - 为什么 BatchNorm 能让训练更稳定；
    - 为什么 Warmup 可以部分修复不稳定训练；
    - 为什么 Warmup 不能完全替代 BatchNorm。

## 二、实验内容

本实验将比较以下三种模型训练情况：

### 实验 1：标准 ResNet-18（含 BatchNorm）

使用标准 ResNet-18，在 CIFAR-10 数据集上训练，记录并绘制 Loss 曲线与准确率曲线。

### 实验 2：去掉 BatchNorm 的 ResNet-18

将 ResNet-18 中的 BatchNorm 层移除，保持其他设置尽量不变，观察 Loss 是否更容易波动。

### 实验 3：去掉 BatchNorm 后加入 Warmup

在实验 2 的基础上加入 Warmup，观察其是否能缓解训练不稳定现象。

## 三、实验原理

### 1\. 什么是训练不稳定

在训练深层神经网络时，如果网络内部各层输入分布变化较大，或者学习率偏大，训练过程可能出现不稳定现象。常见表现有：

- Loss 大幅上下波动
- Loss 长时间不下降
- Accuracy 忽高忽低
- 训练初期出现发散或异常值

这类现象说明模型参数更新过于剧烈，或者梯度传播不稳定。

### 2\. BatchNorm 的作用

BatchNorm 的核心思想是：

对每一层的中间特征做归一化处理，再进行可学习的缩放和平移。

这样做的好处是：

- 让不同层的输入尺度更稳定
- 缓解梯度过大或过小
- 加速网络收敛
- 提高训练稳定性

因此，在 ResNet 这类深层网络中，BatchNorm 通常非常重要。

### 3\. Warmup 的作用

Warmup 的核心思想是：

训练刚开始时，先使用较小的学习率，然后逐步增加到目标学习率。

这样可以避免：

- 参数在初始阶段更新过猛
- 网络还未形成稳定表示时被大步长“冲坏”
- Loss 在训练初期大幅振荡

注意：

Warmup 调整的是优化过程，BatchNorm 调整的是网络内部特征分布。  
两者作用不同。

## 四、实验数据集

本实验使用 **CIFAR-10** 数据集。

### 数据集简介

CIFAR-10 包含 10 个类别的彩色图片，每张图片大小为 32×32，共 60000 张：

- 50000 张训练图片
- 10000 张测试图片

## 五、实验任务

### 任务一：训练标准 ResNet-18（含 BatchNorm）

#### 要求

1.  使用标准 ResNet-18 模型。
2.  在 CIFAR-10 上训练模型。
3.  记录每个 epoch 的：
    - 训练集 loss
    - 训练集 accuracy
    - 测试集 accuracy
4.  绘制曲线图。

#### 观察重点

- Loss 是否平稳下降
- Accuracy 是否持续上升
- 曲线是否比较光滑

### 任务二：训练去掉 BatchNorm 的 ResNet-18

#### 要求

1.  修改 ResNet-18，将所有 BatchNorm 层去掉。
2.  保持数据集、优化器、batch size、epoch 数尽量与任务一一致。
3.  记录并绘制相同指标。

#### 观察重点

与任务一对比，重点观察：

- Loss 是否更容易波动
- 前几个 epoch 是否出现明显震荡
- 最终精度是否下降
- 收敛是否变慢

### 任务三：识别不稳定训练轨迹

请根据你绘制出的曲线，判断哪些训练过程是不稳定的。

#### 可参考以下判据

若出现以下现象，可视为存在不稳定训练轨迹：

1.  Loss 曲线呈明显锯齿状
2.  相邻 epoch 之间 loss 起伏很大
3.  某些时刻 loss 突然升高
4.  accuracy 长时间不上升
5.  前几轮训练表现异常剧烈波动

#### 进一步要求

请你用自己的语言说明：

- 哪一组训练最稳定
- 哪一组最不稳定
- 你的判断依据是什么

### 任务四：对“无 BatchNorm 模型”加入 Warmup

#### 要求

1.  仍使用“去掉 BatchNorm”的 ResNet-18。
2.  在训练中加入 Warmup 学习率策略。
3.  与“不加 Warmup 的无 BN 模型”进行对比。

#### 建议设置

- 前 5 个 epoch 使用 Warmup
- 学习率从较小值线性增加到目标学习率
- 后续进入正常训练阶段

#### 观察重点

- Loss 初期波动是否减小
- 训练是否更容易启动
- 最终效果是否有改善
- 是否仍然不如含 BatchNorm 的模型稳定

## 六、实验步骤

### 第一步：加载数据集

完成以下内容：

1.  下载 CIFAR-10 数据集
2.  对图片进行标准化处理
3.  构造训练迭代器和测试迭代器

### 第二步：实现标准 ResNet-18

你可以采用以下两种方式之一：

#### 方式 A：直接调用 torchvision.models 中的 ResNet-18

适合快速完成实验。

#### 方式 B：参考课程代码，自己实现 ResNet-18

适合理解网络结构。

### 第三步：实现无 BatchNorm 的 ResNet-18

你需要修改残差块 BasicBlock，将其中的 BatchNorm 层移除。

你需要思考：

- 去掉 BN 后，卷积层输出会发生什么变化？
- 为什么训练可能更不稳定？

### 第四步：编写训练函数

训练函数至少应记录：

- 每个 epoch 的 train loss
- 每个 epoch 的 train accuracy
- 每个 epoch 的 test accuracy

建议额外记录：

- 每若干个 batch 的 loss  
    这样更容易观察局部波动。

### 第五步：加入 Warmup

在优化器训练过程中，加入学习率 Warmup 策略。

你需要观察：

- 前几个 epoch 的 loss 是否更平稳
- 模型是否比“无 BN 且无 Warmup”更容易收敛

### 第六步：绘制曲线

至少绘制以下图像：

1.  **三组模型的训练 Loss 曲线**
2.  **三组模型的测试 Accuracy 曲线**

建议增加：

1.  **无 BN 模型加入 Warmup 前后的 Loss 局部放大图**

## 七、实验建议参数

可参考如下参数设置：

- 数据集：CIFAR-10
- 模型：ResNet-18
- batch size：128
- 优化器：SGD
- learning rate：0.1
- momentum：0.9
- weight decay：1e-4
- epoch：20 或 30

如果现象不明显，可以尝试：

- 略微增大学习率
- 重点观察前 5 个 epoch
- 记录更细粒度的 batch loss

## 八、实验结果记录表

请填写下表：

| 模型  | 是否使用 BN | 是否使用 Warmup | 最终训练损失 | 最终测试准确率 | Loss 波动情况 | 是否稳定 |
| --- | --- | --- | --- | --- | --- | --- |
| ResNet-18 | 是   | 否   |     |     |     |     |
| ResNet-18 | 否   | 否   |     |     |     |     |
| ResNet-18 | 否   | 是   |     |     |     |     |

## 九、实验报告要求

请提交一份简要实验报告，内容至少包括以下四部分：

### 1\. 实验目的

说明本实验要解决什么问题。

### 2\. 实验方法

说明你比较了哪几组模型，训练设置如何。

### 3\. 实验结果

给出曲线图、表格，并描述观察到的现象。

### 4\. 实验分析

回答以下问题：

1.  加入 BatchNorm 后，Loss 曲线有什么变化？
2.  去掉 BatchNorm 后，训练出现了哪些不稳定现象？
3.  Warmup 是否改善了训练稳定性？
4.  为什么 Warmup 不能完全替代 BatchNorm？
5.  你认为 BatchNorm 和 Warmup 分别解决了什么问题？

## 十、思考题

请完成以下思考题：

### 思考题 1

为什么 ResNet 已经有残差连接了，仍然需要 BatchNorm？

### 思考题 2

如果把学习率调得很小，无 BatchNorm 的模型还能训练吗？代价是什么？

### 思考题 3

Warmup 是如何改善训练初期稳定性的？

### 思考题 4

如果 batch size 很小，BatchNorm 会不会出现问题？为什么？

### 思考题 5

除了 BatchNorm 和 Warmup，还有哪些办法可以提高训练稳定性？

## 十一、实验提示

### 提示 1

如果你的“无 BN 模型”训练看起来还比较稳定，可能原因有：

- 学习率设置较小
- epoch 数太少
- 训练记录过于粗糙
- 模型实现中仍保留了某些归一化机制

这时可以尝试：

- 增大学习率
- 更频繁记录 batch loss
- 重点观察训练前期

### 提示 2

不要只看最终准确率，还要看：

- 训练是否平稳
- 是否容易收敛
- 是否对学习率敏感

有时两个模型最终精度接近，但训练稳定性差异很大。

### 提示 3

本实验的重点不是“跑到最高精度”，而是：

看懂训练曲线，解释训练现象，理解 BatchNorm 与 Warmup 的作用区别。

## 十五、附加挑战（选做）

如果你完成较快，可以进一步尝试：

1.  改变学习率，观察无 BN 模型对学习率是否更敏感
2.  改变 batch size，观察 BatchNorm 效果是否发生变化
3.  尝试 GroupNorm 或 LayerNorm，与 BatchNorm 比较
4.  观察每个 batch 的 loss，而不是只看 epoch 平均 loss