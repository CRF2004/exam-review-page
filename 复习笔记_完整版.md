# 深度学习期末复习笔记（完整版）

> 教材：《动手学深度学习》PyTorch 第二版 (Aston Zhang et al.)
> 页码对应 PDF 内部页码，非 PDF 阅读器页码

---

## 第一阶段：神经网络基础（第2-4章）

### 第2章 预备知识（p39-83）

> **本章考法**：以**选择题**和**填空**为主，自动微分部分可能考**代码题**（要求手写 backward 流程或解释梯度传播）

#### 2.1 数据操作（p40-47）
- **张量（Tensor）**：n维数组，0维=标量，1维=向量，2维=矩阵
- **常见操作**：`torch.arange()`, `torch.zeros()`, `torch.ones()`, `torch.randn()`, `.shape`, `.reshape()`
- **广播机制**（p44）：形状不同的张量按元素运算时自动扩展
  - 🎯 **考法**：给两个不同形状张量问运算结果形状（如 `(3,1)+(1,4)` → `(3,4)`）
- **索引和切片**（p45）：`X[1:3]`, `X[1:3, 1:3]`
- **节省内存**（p46）：`Y[:] = X + Y` 或 `Y += X` 原地操作
- **转换**（p47）：`X.numpy()`, `torch.tensor(A)`

#### 2.2 数据预处理（p47-49）
- 读取 CSV：`pd.read_csv()`
- 处理缺失值：`inputs.fillna(value)`，将类别转为数值（`pd.get_dummies()`）
- 🎯 **考法**：给一段缺失值数据问如何处理

#### 2.3 线性代数（p50-62）
- **标量、向量、矩阵、张量**
- **转置**：`A.T`
- **降维**：`.sum(axis=0)`（按列求和，消去行）
- **范数**：L1范数 `torch.abs(u).sum()`，L2范数 `torch.norm()`
- **点积**、**矩阵-向量积**、**矩阵-矩阵乘法**
- 🎯 **考法**：范数计算、矩阵乘法维度判断

#### 2.4 微积分（p63-68）
- **导数**、**偏导数**、**梯度**、**链式法则**
- 🎯 **考法**：**必考简答**——用链式法则推导某个复合函数的梯度

#### 2.5 自动微分（p69-72）⭐代码考点
- `x.requires_grad_(True)` 开启梯度追踪
- `y.backward()` 反向传播求导
- `x.grad` 访问梯度
- **分离计算**：`y = x.detach()` 阻断梯度传播
- **非标量反向传播**：`y.sum().backward()` 或传梯度参数
- 🎯 **考法**：代码题——给一段 autograd 代码问运行后 `x.grad` 的值，或解释 `detach()` 的作用

#### 2.6 概率（p73-80）
- 基本概率论、条件概率、贝叶斯定理
- 期望、方差、独立性
- 🎯 **考法**：选择题概念辨析

---

### 第3章 线性神经网络（p85-126）

> **本章考法**：**核心章**，线性回归/SGD 简答必考，softmax 导数推导常考，代码实现题高频

#### 3.1 线性回归（p85-95）⭐核心
- **模型**：$\hat{y} = Xw + b$
- **损失函数**：均方损失 $L(w,b) = \frac{1}{2n}\sum_i(\hat{y}^{(i)}-y^{(i)})^2$ (p87)
  - 🎯 **考法**：简答——解释为什么 MSE 前有 1/2（为了求导消去系数）
- **解析解**：$w^* = (X^TX)^{-1}X^Ty$
  - 🎯 **考法**：问何时有解析解（满秩时）& 为什么深度学习不用（高维不可逆、计算量大）
- **小批量随机梯度下降** (p88)：$w ← w - η\frac{1}{|B|}\sum_i∂_wL^{(i)}$
  - 🎯 **考法**：**必考简答/推导**——手推线性回归梯度，比较 GD / SGD / Mini-batch SGD
- **正态分布与平方损失**（p91）：
  - 🎯 **考法**：推导——从极大似然估计导出 MSE 损失函数

#### 3.2 线性回归从零实现（p95-100）⭐代码考点
- 🎯 **考法**：**大概率手写代码**——生成数据集 → 定义模型 → 定义损失 → SGD 更新 → 训练循环

#### 3.3 线性回归简洁实现（p100-104）
- `nn.Linear(in, out)`、`nn.MSELoss()`、`optim.SGD()`
- 🎯 **考法**：对比从零实现 vs 简洁实现

#### 3.4 Softmax 回归（p105-111）⭐核心
- **分类问题** → 多类分类
- **Softmax 运算** (p107)：$\hat{y}_j = \frac{exp(o_j)}{\sum_k exp(o_k)}$
- **交叉熵损失** (p108)：$L = -\sum_j y_j log\hat{y}_j = -log\hat{y}_c$
  - 🎯 **考法**：**必考推导**——手推 softmax 的导数 ∂L/∂o_j（两种情形：j=c 和 j≠c）
- **信息论基础** (p109)：自信息、熵、KL散度
  - 🎯 **考法**：选择题——概念辨析（交叉熵 = 熵 + KL散度）

#### 3.5 图像分类数据集（p111-114）
- Fashion-MNIST：10类，28×28灰度图
- 🎯 **考法**：选择题——Fashion-MNIST 的图片尺寸和类别数

#### 3.6-3.7 Softmax 从零/简洁实现（p115-126）⭐代码考点
- softmax 稳定版：`exp(X - max(X)) / sum(...)`
- `nn.CrossEntropyLoss()` = LogSoftmax + NLLLoss
- 分类精度计算
- 🎯 **考法**：代码题——要求实现 softmax 和交叉熵损失计算

---

### 第4章 多层感知机（p127-188）⭐核心

> **本章考法**：**绝对重点**，过拟合/欠拟合辨析、梯度消失/爆炸、激活函数对比是简答必考，MLP 代码实现可能考

#### 4.1 多层感知机 MLP（p127-133）
- **隐藏层**：$H = σ(XW^{(1)} + b^{(1)})$, $O = HW^{(2)} + b^{(2)}$
- **为什么需要激活函数**：无激活则多层等价于单层（线性变换的复合仍是线性）
- **激活函数**（p130-133）：
  - ReLU：$σ(x) = max(0, x)$
  - Sigmoid：$σ(x) = 1/(1+e^{-x})$
  - Tanh：$tanh(x) = (e^x-e^{-x})/(e^x+e^{-x})$
- 🎯 **考法**：**简答必考**——比较 ReLU/Sigmoid/Tanh 的优缺点，解释为什么 ReLU 能缓解梯度消失

#### 4.2-4.3 MLP 实现（p134-138）
- 🎯 **考法**：代码题——用 `nn.Sequential` 搭建一个 MLP（含 ReLU）

#### 4.4 过拟合和欠拟合（p139-149）⭐核心概念
- **训练误差 vs 泛化误差** (p140)
- **模型选择**：验证集、K折交叉验证 (p142)
- **过拟合**：模型过于复杂 > 数据量
- **欠拟合**：模型不够复杂
- **影响因素**：模型复杂度、训练集大小、正则化强度
- 🎯 **考法**：**简答必考**——给学习曲线判断过/欠拟合及解决策略（过拟合→正则化/增数据/降复杂度；欠拟合→增复杂度/降正则化）

#### 4.5 权重衰减（Weight Decay / L2正则化）（p149-155）
- $L(w,b) + \frac{λ}{2}||w||^2$
- $w ← w - η(∂L/∂w + λw)$
- 🎯 **考法**：推导——给损失加 L2 正则后更新公式如何变化？对比 L1/L2 正则的区别（L1 产生稀疏解）

#### 4.6 Dropout 暂退法（p156-161）
- 前向传播随机丢弃神经元（概率 p），测试时丢弃乘 (1-p)
- 🎯 **考法**：**简答必考**——Dropout 的原理，训练和测试时的区别

#### 4.7 前向传播、反向传播和计算图（p162-165）
- 前向 → 损失 → 反向 → 更新
- 🎯 **考法**：**推导题必考**——给一个简单网络（如 MLP），画出计算图，手推反向传播梯度

#### 4.8 数值稳定性和模型初始化（p166-169）⭐重要
- **梯度消失**：sigmoid/tanh 饱和区 → 梯度→0，深层不学习
- **梯度爆炸**：梯度指数增长 → 参数溢出 NaN
- **参数初始化**：
  - Xavier初始化：$Var(W) = 2/(n_{in} + n_{out})$ → 适合sigmoid/tanh
  - He初始化：$Var(W) = 2/n_{in}$ → 适合ReLU
- 🎯 **考法**：**简答必考**——梯度消失/爆炸的原因和解决（适当初始化、BN、ReLU、残差连接）

#### 4.9 环境与分布偏移（p170-179）
- **协变量偏移**、**标签偏移**、**概念偏移**
- 🎯 **考法**：选择题——给场景判断偏移类型

---

## 第二阶段：卷积神经网络与 CV 应用（第6-7章、第13章）

### 第6章 卷积神经网络（p217-246）⭐核心

> **本章考法**：公式计算（输出尺寸）是计算题重点，各架构特点简答，LeNet 代码可能考

#### 6.1 从全连接层到卷积（p218-221）
- **平移不变性**、**局部性**
- **卷积 vs 全连接**：参数从 O(hw·HW) 降到 O(k²)
- 🎯 **考法**：简答——为什么 CNN 比 FC 参数少？（权值共享 + 局部连接）

#### 6.2 图像卷积（p222-227）⭐核心
- **互相关运算**（实际使用时互相关 = 卷积核翻转，但可学习所以等价）
- **边缘检测** (p225)
- **感受野** (p227)
- 🎯 **考法**：简答——互相关与卷积的区别？选择题——计算感受野大小

#### 6.3 填充和步幅（p228-232）⭐代码考点
- **输出尺寸公式**：$⌊(n_h - k_h + p_h + s_h)/s_h⌋ × ⌊(n_w - k_w + p_w + s_w)/s_w⌋$
- 🎯 **考法**：**计算题必考**——给定输入尺寸 n、核大小 k、填充 p、步幅 s，计算输出尺寸（如输入 32×32，k=5，p=2，s=1 → 32×32，same conv）

#### 6.4 多输入多输出通道（p233-236）
- 核形状：$(c_o, c_i, k_h, k_w)$
- **1×1 卷积** (p235)：相当于全连接层
- 🎯 **考法**：计算题——给定输入通道数和输出通道数，问卷积核参数量

#### 6.5 汇聚层（Pooling）（p236-240）
- **最大汇聚** vs **平均汇聚**
- 作用：降维、平移不变性、防过拟合
- 🎯 **考法**：简答——池化的作用？为什么能带来平移不变性？

#### 6.6 LeNet（p240-245）
- Conv → Pool → Conv → Pool → FC → FC → FC
- 🎯 **考法**：**简答必考**——画出 LeNet 架构图，说明每层参数

#### 关键公式：
$O = (W - K + 2P) / S + 1$

---

### 第7章 现代卷积神经网络（p247-288）⭐架构演进

> **本章考法**：各网络架构对比是简答大题，BatchNorm 原理必考，ResNet 残差思想必考

#### 7.1 AlexNet（p248-255）⭐重要
- **创新**：ReLU、Dropout、数据增强、GPU 并行
- 🎯 **考法**：简答——AlexNet vs LeNet 的改进（更深+ReLU+Dropout）

#### 7.2 VGG（p255-258）
- VGG块：多个 3×3 conv + 2×2 pool
- 两个 3×3 = 一个 5×5 感受野
- 🎯 **考法**：简答——为什么要用小卷积核堆叠代替大卷积核？（更少参数 + 更多非线性 + 相同感受野）

#### 7.3 NiN（p259-262）
- 1×1 conv（MLPConv）+ 全局平均池化
- 🎯 **考法**：选择题——NiN 的创新点

#### 7.4 GoogLeNet（p263-267）
- **Inception 块**：1×1, 3×3, 5×5 conv + 3×3 pool 并行 → 通道拼接
- 1×1 conv 做 bottleneck 降维
- 🎯 **考法**：简答——Inception 块的设计思想（多尺度并行）

#### 7.5 批量规范化 BatchNorm（p268-274）⭐核心
- **公式**：$\hat{x} = (x - μ_B) / \sqrt{σ_B^2 + ε}$，$y = γ\hat{x} + β$
- **训练 vs 推理**：训练用 batch 统计，推理用全局移动平均
- 🎯 **考法**：**简答必考**——BN 原理、训练/推理行为差异、为什么 BN 能加速训练

#### 7.6 ResNet（p275-288）⭐最重要
- **退化问题** vs 过拟合
- **残差块**：$y = F(x) + x$
- **瓶颈结构**：1×1 → 3×3 → 1×1
- 🎯 **考法**：**简答必考**——为什么 ResNet 能训练更深的网络？（恒等映射保证梯度直通）
  - 推导——$∂Loss/∂x_l = ∂Loss/∂x_L · (1 + ∂/∂x_l ∑F)$，梯度不会消失

---

### 第13章 计算机视觉（p549-648）

> **本章考法**：微调流程简答，目标检测各模型对比，SSD/R-CNN 系列选择或简答

#### 13.1 图像增广（p549-557）
- 🎯 **考法**：选择题——哪些增广方法、训练/测试区别

#### 13.2 微调 Fine-Tuning（p557-563）⭐核心
- **步骤**：预训练 → 替换输出层 → 输出层随机初始化 → 微调
- 🎯 **考法**：**简答必考**——微调的步骤和为什么有效（预训练特征通用，下游只需少量调整）

#### 13.3 目标检测基础（p564-587）
- **边界框**、**锚框**、**IoU**、**NMS**
- 🎯 **考法**：计算题——给定两个框计算 IoU

#### 13.7 SSD（p588-599）⭐重要
- 多尺度特征图检测
- 🎯 **考法**：简答——SSD 如何实现多尺度检测？

#### 13.8 R-CNN 系列（p599-605）
- **R-CNN** → **Fast R-CNN**（共享 CNN） → **Faster R-CNN**（RPN） → **Mask R-CNN**
- 🎯 **考法**：简答——R-CNN 到 Faster R-CNN 的演进（速度提升原因）

#### 13.10 转置卷积（p612-617）
- 🎯 **考法**：选择题——什么是转置卷积，与反卷积的区别

---

## 第三阶段：循环神经网络与序列建模（第8-9章、第14-15章）

### 第8章 循环神经网络（p289-332）⭐核心

> **本章考法**：RNN 公式推导简答必考，BPTT 原理必考，Perplexity 选择题

#### 8.1 序列模型（p290-298）
- **自回归模型**、**马尔可夫模型**
- 🎯 **考法**：选择题——τ 阶马尔可夫的含义

#### 8.2 文本预处理（p298-302）
- 词元化 → 词表 → 数值化
- 🎯 **考法**：选择题——`<unk>`, `<pad>`, `<bos>`, `<eos>` 的作用

#### 8.3 语言模型（p303-312）
- **n元语法** (p305)
- **马尔可夫假设**
- 🎯 **考法**：计算题——给定语料计算 n-gram 概率

#### 8.4 循环神经网络（p312-317）⭐核心
- **隐藏状态**：$H_t = φ(X_tW_{xh} + H_{t-1}W_{hh} + b_h)$
- **参数共享**
- **困惑度 Perplexity** (p315)：$exp(-\frac{1}{n}\sum logP(x_t|x_{<t}))$
- 🎯 **考法**：**必考**——RNN 前向公式手写、Perplexity 含义（越小越好, 1 为理想）

#### 8.5 RNN 从零实现（p317-325）⭐代码考点
- **梯度裁剪** (p321)
- 🎯 **考法**：代码题——梯度裁剪公式和代码

#### 8.7 通过时间反向传播 BPTT（p329-332）
- 🎯 **考法**：**简答必考**——RNN 的梯度消失/爆炸（与时间步数指数相关），对比 CNN 的梯度问题

---

### 第9章 现代循环神经网络（p333-381）

> **本章考法**：GRU/LSTM 门控公式对比简答必考，Seq2Seq+注意力简答，Beam Search 概念

#### 9.1 GRU（p333-341）⭐核心
- **重置门** $R_t$、**更新门** $Z_t$
- 🎯 **考法**：**简答必考**——GRU 如何缓解梯度消失？（更新门接近 1 时保留长期信息）

#### 9.2 LSTM（p341-349）⭐核心
- **遗忘门** $F_t$、**输入门** $I_t$、**输出门** $O_t$
- **记忆细胞** $C_t$、**隐藏状态** $H_t$
- 🎯 **考法**：**必考**——LSTM 三个门的公式手写；简答——对比 LSTM 与 GRU（LSTM 有独立记忆细胞，GRU 更少参数）

#### 9.3 深度 RNN（p349-352）
- 🎯 **考法**：选择题

#### 9.4 双向 RNN（p352-357）
- 🎯 **考法**：**易错题**——问能否用于语言模型？（不能，因为看到了未来的词）

#### 9.6 编码器-解码器架构（p364-366）
- 🎯 **考法**：简答——Encoder-Decoder 结构

#### 9.7 Seq2Seq（p366-377）⭐重要
- Teacher Forcing (p373)
- **BLEU** 评分 (p375)
  - $BLEU = exp(min(0, 1 - \frac{len_{pred}}{len_{label}})) · \prod_{n=1}^k p_n^{1/2^n}$
- 🎯 **考法**：简答——Teacher Forcing 是什么？计算——BLEU 计算

#### 9.8 束搜索 Beam Search（p377-380）
- k=1 = 贪心搜索
- 🎯 **考法**：简答——Beam Search vs 贪心搜索 vs 穷举搜索

---

### 第14章 自然语言处理：预训练（p649-702）

> **本章考法**：BERT 两个预训练任务是重点，Word2Vec 概念选择题

#### 14.1-14.2 Word2Vec（p650-656）
- **Skip-Gram** vs **CBOW**
- **负采样** (p654)、**层序 Softmax** (p655)
- 🎯 **考法**：选择题——Skip-Gram 和 CBOW 的区别

#### 14.8 BERT（p679-691）⭐核心
- **MLM**：mask 15% 词元（80%→[MASK]，10%→随机，10%→不变）
- **NSP**：下句预测
- **输入表示**：Token + Segment + Position Embedding
- [CLS] [SEP] 标记作用
- 🎯 **考法**：**简答必考**——BERT 两个预训练任务；为什么 MLM 比标准语言模型更好（双向上下文）；BERT 输入表示的三部分

---

### 第15章 自然语言处理：应用（p703-740）

> **本章考法**：BERT 微调方式简答

#### 15.1-15.3 情感分析（p704-718）
- RNN 分类、TextCNN 分类
- 🎯 **考法**：选择题——TextCNN 的使用

#### 15.6 微调 BERT（p731-740）
- 单文本分类：`[CLS]` → FC
- 文本对分类：`[CLS] A [SEP] B [SEP]` → FC
- 文本标注：每 token 表示 → FC
- 🎯 **考法**：简答——如何根据任务类型（分类/标注/对分类）调整 BERT 输出层

---

## 第四阶段：注意力机制与性能优化（第10-12章）

### 第10章 注意力机制（p381-426）⭐最重要

> **本章考法**：Transformer 架构是**整本书最重要**的考点，多头注意力/自注意力/位置编码均可能考简答或推导

#### 10.1-10.2 注意力基础（p382-392）
- **查询-键-值（QKV）框架**
- 🎯 **考法**：简答——注意力机制的 QKV 框架

#### 10.3 注意力评分函数（p393-398）
- **加性注意力**：$a(q,k) = w_v^T tanh(W_qq + W_kk)$
- **缩放点积注意力**：$a(q,k) = q^Tk/\sqrt{d}$
- 🎯 **考法**：简答——为什么缩放点积要除以 $\sqrt{d}$？（防 softmax 进入小梯度区域）；什么时候用加性什么时候用点积？

#### 10.4 Bahdanau 注意力（p399-403）
- 🎯 **考法**：简答——Bahdanau 注意力如何改进 Seq2Seq？（不依赖单一上下文向量，每步动态关注不同源位置）

#### 10.5 多头注意力（p404-408）⭐核心
- $MultiHead(Q,K,V) = Concat(head_1, ..., head_h)W_O$
- 🎯 **考法**：**简答必考**——为什么需要多头注意力？（多子空间并行学习不同模式）

#### 10.6 自注意力和位置编码（p408-413）⭐核心
- **自注意力**：Q=K=V=X
- **复杂度对比**：
  - 自注意力：$O(n^2d)$
  - RNN：$O(nd^2)$
  - CNN：$O(knd^2)$
- **位置编码**：$PE_{(pos,2i)} = sin(pos/10000^{2i/d})$, $PE_{(pos,2i+1)} = cos(pos/10000^{2i/d})$
- 🎯 **考法**：**简答必考**——为什么自注意力需要位置编码？（置换不变性，没有序列位置信息）；自注意力 vs RNN vs CNN 的复杂度对比

#### 10.7 Transformer（p413-426）⭐最重要

| 组件 | 说明 | 考法 |
|------|------|------|
| 编码器层 | 多头自注意力 → Add&Norm → FFN → Add&Norm | 🎯 画架构图 |
| 解码器层 | 掩蔽多头自注意力 → Add&Norm → 交叉注意力 → Add&Norm → FFN → Add&Norm | 🎯 解释掩蔽原因 |
| FFN | $max(0, xW_1 + b_1)W_2 + b_2$ | 选择题 |
| 残差连接 | $x + Sublayer(x)$ | 简答——为什么？ |
| 层规范化 LayerNorm | 同层隐藏单元归一化 | 简答——LN vs BN |
| 掩蔽多头注意力 | 上三角 mask，防看未来 | 简答——为什么？ |

- 🎯 **考法**：**终极必考题**——画出 Transformer 架构图（编码器+解码器），标注每个子层；为什么用 LayerNorm 而不用 BatchNorm？（序列变长时 BN 统计不稳定，LN 独立于 batch）

---

### 第11章 优化算法（p427-502）

> **本章考法**：各优化器的对比简答，Adam 公式重点，SGD/Momentum/Adam 选择可能是大题

#### 11.1 优化与深度学习（p427-433）
- **局部最小值** vs **鞍点**
- 🎯 **考法**：选择题——高维空间中更常遇到的是鞍点而非局部最小

#### 11.5 小批量 SGD（p455-466）
- 批量大小的影响
- 🎯 **考法**：简答——batch size 对训练的影响（大 batch→梯度准但泛化差；小 batch→噪声大但正则化好）

#### 11.6 动量法 Momentum（p466-476）
- $v_t ← βv_{t-1} + (1-β)g_t$，$w ← w - ηv_t$
- 🎯 **考法**：简答——Momentum 解决 SGD 的什么问题？（震荡 + 收敛慢）

#### 11.7 AdaGrad（p476-481）
- $s_t = s_{t-1} + g_t^2$
- 🎯 **考法**：简答——AdaGrad 的缺点（学习率单调递减到 0）

#### 11.8 RMSProp（p482-485）
- $s_t = γs_{t-1} + (1-γ)g_t^2$
- 🎯 **考法**：简答——RMSProp 如何改进 AdaGrad

#### 11.10 Adam（p488-493）⭐最常用
- **一阶矩** + **二阶矩** + **偏差校正**
- $v_t = β_1v_{t-1} + (1-β_1)g_t$
- $s_t = β_2s_{t-1} + (1-β_2)g_t^2$
- $\hat{v}_t = v_t/(1-β_1^t)$, $\hat{s}_t = s_t/(1-β_2^t)$
- $w ← w - η·\hat{v}_t/(\sqrt{\hat{s}_t} + ε)$
- 默认：$β_1=0.9, β_2=0.999$
- 🎯 **考法**：**必考简答/推导**——写出 Adam 更新公式，解释偏差校正的作用（初始时刻矩估计偏零）

#### 11.11 学习率调度器（p493-502）
- 🎯 **考法**：选择题——常见调度策略（阶梯下降、余弦退火等）

#### 优化器对比总结

| 优化器 | 核心思想 | 优点 | 缺点 |
|--------|---------|------|------|
| SGD | 原始梯度 | 简单 | 收敛慢，震荡 |
| Momentum | 惯性 | 加速，减震荡 | 超参数 β |
| AdaGrad | 自适应学习率 | 稀疏特征大步 | 学习率→0 |
| RMSProp | 指数加权 | 克服单调衰减 | 无动量 |
| Adam | 动量 + RMSProp | 最常用，鲁棒 | 可能不收敛（已修复） |

---

### 第12章 计算性能（p503-548）

> **本章考法**：CPU/GPU 差异选择题，数据并行概念简答

#### 12.4 硬件（p517-527）
- **CPU**：少量核心，适合串行
- **GPU**：大量核心，适合并行矩阵运算
- 🎯 **考法**：选择题——为什么 GPU 适合深度学习？（SIMT 架构，矩阵运算并行度高）

#### 12.5 多GPU训练（p528-536）
- **数据并行**：每 GPU 一份模型，分不同子批量
- 🎯 **考法**：简答——数据并行步骤（前向→损失→反向→梯度 AllReduce→更新）

#### 12.7 参数服务器（p540-548）
- **Ring AllReduce** (p542)
- 🎯 **考法**：选择题

---

## 题型分布速查表

### 🅰 选择题 / 填空题（概念辨析类）
| 考点 | 章节 |
|------|------|
| 广播机制计算结果 | §2.1 |
| 梯度消失/爆炸原因 | §4.8 |
| 过拟合/欠拟合判断 | §4.4 |
| 激活函数曲线 | §4.1 |
| 卷积/池化输出尺寸 | §6.3 |
| 各种 Pooling 的作用 | §6.5 |
| CNN 网络架构演进 | §7.1-7.6 |
| 分布偏移类型判断 | §4.9 |
| 优化器对比选择 | §11 |
| 注意力评分函数选择 | §10.3 |
| CPU/GPU 对比 | §12.4 |
| 困惑度含义 | §8.4 |
| n-gram | §8.3 |
| Skip-Gram vs CBOW | §14.1 |
| 门控机制 | §9.1-9.2 |

### 🅱 简答题（原理阐述 + 推导类）
| 必考题目 | 章节 |
|----------|------|
| 链式法则推导梯度 | §2.4-2.5 |
| softmax + 交叉熵求导 | §3.4 |
| SGD vs Mini-batch vs GD | §3.1 |
| 过拟合解决方案 | §4.4 |
| 梯度消失/爆炸 | §4.8 |
| 激活函数对比 | §4.1 |
| Dropout 原理 | §4.6 |
| BatchNorm 原理 | §7.5 |
| ResNet 为什么有效 | §7.6 |
| LSTM / GRU 公式 + 对比 | §9.1-9.2 |
| BERT 预训练任务 | §14.8 |
| Transformer 架构图 | §10.7 |
| 自注意力复杂度 | §10.6 |
| 为什么需要位置编码 | §10.6 |
| Adam 公式 | §11.10 |
| 微调步骤 | §13.2 |

### 🅲 代码题（手写实现类）
| 必考题目 | 章节 |
|----------|------|
| 线性回归从零实现 | §3.2 |
| Softmax + 交叉熵 | §3.6-3.7 |
| MLP 搭建 | §4.2-4.3 |
| LeNet / CNN 搭建 | §6.6 |
| RNN 训练循环 | §8.5-8.6 |
| 梯度裁剪 | §8.5 |
| autograd 梯度传播分析 | §2.5 |

---

## 附录：PyTorch API 速查

| 函数 | 用途 | 教材位置 |
|------|------|---------|
| `nn.Linear(in, out)` | 全连接层 | p101 |
| `nn.ReLU()` / `nn.Sigmoid()` / `nn.Tanh()` | 激活函数 | p133, p137 |
| `nn.Conv2d(c_in, c_out, k, p, s)` | 卷积层 | p226 |
| `nn.MaxPool2d(k, s)` / `nn.AvgPool2d()` | 汇聚层 | p239 |
| `nn.BatchNorm1d/2d(num_features)` | 批归一化 | p273 |
| `nn.Dropout(p)` | Dropout | p161 |
| `nn.RNN()` / `nn.LSTM()` / `nn.GRU()` | 循环网络 | p326, p346, p340 |
| `nn.CrossEntropyLoss()` | 交叉熵损失 | p124 |
| `nn.MSELoss()` | 均方损失 | p104 |
| `optim.SGD()` / `optim.Adam()` | 优化器 | p103, p490 |
| `nn.MultiheadAttention(embed_dim, nhead)` | 多头注意力 | p407 |
| `nn.TransformerEncoderLayer` | Transformer 编码器层 | p418 |
| `nn.DataParallel(model, device_ids)` | 多 GPU | p537 |

---

*注：页码对应中文 PDF 内部页码，与教材印刷页码一致。🎯 标注了每种考点的常见考试形式。*
