# 实验报告

**课程名称：** 深度学习  
**实验名称：** 实验六：BatchNorm 对 ResNet-18 训练稳定性的影响  
**实验日期：** 2026/04/17  
**实验地点：** 本地计算环境

## 1 实验目的

1. 理解 BatchNorm（批量归一化）在深层网络训练中的作用
2. 观察加入 BatchNorm 前后，ResNet-18 的 Loss 曲线差异
3. 学会识别不稳定训练轨迹的典型表现
4. 理解 Warmup（学习率预热）如何缓解训练初期震荡
5. 能够从实验结果中总结：
   - 为什么 BatchNorm 能让训练更稳定
   - 为什么 Warmup 可以部分修复不稳定训练
   - 为什么 Warmup 不能完全替代 BatchNorm

## 2 实验原理

### 2.1 训练不稳定现象

在训练深层神经网络时，如果网络内部各层输入分布变化较大，或者学习率偏大，训练过程可能出现不稳定现象。常见表现有：

- Loss 大幅上下波动
- Loss 长时间不下降
- Accuracy 忽高忽低
- 训练初期出现发散或异常值

这类现象说明模型参数更新过于剧烈，或者梯度传播不稳定。

### 2.2 BatchNorm 的作用

BatchNorm 的核心思想是对每一层的中间特征做归一化处理，再进行可学习的缩放和平移：

$$\hat{x} = \frac{x - \mu_B}{\sqrt{\sigma_B^2 + \epsilon}}$$

$$y = \gamma \hat{x} + \beta$$

其中 $\mu_B$ 和 $\sigma_B^2$ 是当前 batch 的均值和方差，$\gamma$ 和 $\beta$ 是可学习参数。

BatchNorm 的好处：
- 让不同层的输入尺度更稳定
- 缓解梯度过大或过小
- 加速网络收敛
- 提高训练稳定性

在 ResNet 这类深层网络中，BatchNorm 通常非常重要。

### 2.3 Warmup 的作用

Warmup 的核心思想是训练刚开始时，先使用较小的学习率，然后逐步增加到目标学习率：

$$\text{lr}_t = \text{lr}_{\text{target}} \times \frac{t}{T_{\text{warmup}}}$$

其中 $t$ 是当前 epoch，$T_{\text{warmup}}$ 是 warmup 的总 epoch 数。

Warmup 可以避免：
- 参数在初始阶段更新过猛
- 网络还未形成稳定表示时被大步长"冲坏"
- Loss 在训练初期大幅振荡

注意：Warmup 调整的是优化过程，BatchNorm 调整的是网络内部特征分布，两者作用不同。

## 3 实验步骤

### 3.1 实验环境

- PyTorch 版本：2.x
- Python 版本：3.12
- 训练设备：Kaggle GPU (Tesla T4)
- 实验代码：`script/exp6_kaggle.py`

### 3.2 实验方法

本实验采用控制变量法，比较三种 ResNet-18 变体，核心差异如下：

```python
# 实验一：标准 BasicBlock（含 BatchNorm）
class BasicBlock(nn.Module):
    def __init__(self, in_planes, planes, stride=1):
        self.conv1 = nn.Conv2d(in_planes, planes, 3, stride, 1, bias=False)
        self.bn1   = nn.BatchNorm2d(planes)
        self.conv2 = nn.Conv2d(planes, planes, 3, 1, 1, bias=False)
        self.bn2   = nn.BatchNorm2d(planes)
        # shortcut 同样含 BN

# 实验二/三：去掉 BatchNorm（bias=True，无 BN）
class BasicBlockNoBN(nn.Module):
    def __init__(self, in_planes, planes, stride=1):
        self.conv1 = nn.Conv2d(in_planes, planes, 3, stride, 1, bias=True)
        self.conv2 = nn.Conv2d(planes, planes, 3, 1, 1, bias=True)
        # 无 BN 层，shortcut 也无 BN
```

训练循环中通过 Warmup 线性增大学习率：

```python
# 实验三的 Warmup 逻辑（前 5 epoch）
if use_warmup and epoch < warmup_epochs:
    warmup_lr = base_lr * (epoch + 1) / warmup_epochs  # 0.02→0.1
    for pg in optimizer.param_groups:
        pg['lr'] = warmup_lr
```

固定参数：SGD(lr=0.1, momentum=0.9, weight_decay=1e-4)，batch_size=128，epochs=20。

通过绘制 Loss/Accuracy 曲线对比分析三种配置下的训练稳定性差异。

### 3.3 数据集

使用 CIFAR-10 数据集：
- 训练集：50000 张 32×32 彩色图片
- 测试集：10000 张 32×32 彩色图片
- 类别数：10

数据预处理：
- 训练集：随机裁剪、随机水平翻转、归一化
- 测试集：归一化

### 3.4 模型架构

三组实验均使用 ResNet-18 骨干，差异仅在于 BasicBlock 是否含 BN（见 3.2 节）。整体结构如下：

```python
class ResNet18(nn.Module):
    def __init__(self, block, num_blocks=[2,2,2,2], num_classes=10):
        self.in_planes = 64
        self.conv1  = nn.Conv2d(3, 64, 3, 1, 1, bias=False)
        self.bn1    = nn.BatchNorm2d(64)          # 实验二/三中移除
        self.layer1 = self._make_layer(block, 64,  num_blocks[0], stride=1)
        self.layer2 = self._make_layer(block, 128, num_blocks[1], stride=2)
        self.layer3 = self._make_layer(block, 256, num_blocks[2], stride=2)
        self.layer4 = self._make_layer(block, 512, num_blocks[3], stride=2)
        self.linear = nn.Linear(512, num_classes)

    def _make_layer(self, block, planes, num_blocks, stride):
        strides = [stride] + [1] * (num_blocks - 1)
        layers = []
        for stride in strides:
            layers.append(block(self.in_planes, planes, stride))
            self.in_planes = planes * block.expansion
        return nn.Sequential(*layers)

    def forward(self, x):
        out = torch.relu(self.bn1(self.conv1(x)))  # 实验二/三: torch.relu(self.conv1(x))
        out = self.layer1(out)
        out = self.layer2(out)
        out = self.layer3(out)
        out = self.layer4(out)
        out = F.avg_pool2d(out, 4)
        return self.linear(out.view(out.size(0), -1))

# 实验一: model = ResNet18(BasicBlock)         # 含 BN
# 实验二: model = ResNet18NoBN(BasicBlockNoBN) # 无 BN
# 实验三: model = ResNet18NoBN(BasicBlockNoBN) # 无 BN + Warmup
```

### 3.5 训练参数

| 参数 | 值 |
|------|-----|
| Batch size | 128 |
| 优化器 | SGD |
| 学习率 | 0.1 |
| Momentum | 0.9 |
| Weight decay | 1e-4 |
| Epochs | 20 |
| Warmup epochs | 5（仅实验三） |

## 4 实验结果

### 4.1 训练过程记录

三个模型均训练 20 个 epoch，关键数据记录如下：

**实验一（With BN）**：训练损失从 1.79 降至 0.23，测试准确率最高达 87.54%，最终 85.86%

**实验二（No BN）**：训练损失从 1.88 降至 0.34，测试准确率最高达 85.32%，最终 85.25%

**实验三（No BN + Warmup）**：训练损失从 1.97 降至 0.35，测试准确率最高达 86.02%，最终 84.67%

### 4.2 实验结果汇总表

| 模型 | 是否使用 BN | 是否使用 Warmup | 最终训练损失 | 最终测试准确率 | Loss 波动情况（标准差） | 是否稳定 |
|------|-------------|----------------|--------------|----------------|------------------------|----------|
| ResNet-18 | 是 | 否 | 0.2322 | 85.86% | 0.3953 | 是 |
| ResNet-18 | 否 | 否 | 0.3428 | 85.25% | 0.3976 | 否 |
| ResNet-18 | 否 | 是 | 0.3549 | 84.67% | 0.4468 | 否 |

### 4.3 Loss 曲线对比

![训练 Loss 曲线对比](../results/exp6/exp6_results.png)

从 Loss 曲线可以观察到：

1. **收敛速度**：With BN 的模型收敛最快，20 个 epoch 后 Loss 降至 0.23；无 BN 的两个模型最终 Loss 约 0.34-0.35
2. **初期表现**：Warmup 在训练初期（前 5 epoch）确实让 Loss 下降更平缓，避免了大幅震荡
3. **整体趋势**：三条曲线均单调下降，未出现大幅波动，说明学习率设置合理

### 4.4 Accuracy 曲线对比

从测试准确率曲线可以观察到：

1. **With BN 领先全程**：从第 2 个 epoch 开始测试准确率就明显高于另外两个模型
2. **No BN 追赶快**：无 BN 的模型在前 10 个 epoch 提升迅速，后期增长放缓
3. **Warmup 初期落后**：由于前 5 epoch 使用较小学习率，Warmup 组初期准确率明显偏低，后期逐渐追上
4. **最终性能接近**：三组最终测试准确率均在 85% 左右，差距在 1.2% 以内

## 5 实验分析

### 5.1 BatchNorm 对训练稳定性的影响

从实验结果看，BatchNorm 对训练稳定性的提升主要体现在：

1. **更快的收敛速度**：With BN 的模型最终训练损失为 0.2322，明显低于无 BN 模型的 0.34-0.35
2. **更高的测试准确率**：With BN 达到 85.86%，略高于无 BN 的 85.25% 和 84.67%
3. **更小的 Loss 波动**：标准差为 0.3953，略低于无 BN 的 0.3976 和 0.4468

**为什么 BatchNorm 能让训练更稳定？**

1. **规范化内部协变量**：BN 对每一层的输入进行归一化，使得各层输入分布保持稳定，减少 Internal Covariate Shift
2. **梯度传播更稳定**：归一化后，梯度在反向传播时不容易出现过大或过小的情况
3. **允许更大学习率**：BN 使得网络对超参数选择更不敏感，可以使用相对较大的学习率加速训练

### 5.2 去掉 BatchNorm 后的不稳定现象

从实验数据来看，去掉 BatchNorm 后并没有出现剧烈的不稳定现象，主要原因可能是：

1. **学习率设置合理**：0.1 的学习率对于 ResNet-18 来说不算过大
2. **网络深度适中**：ResNet-18 相对较浅，即使没有 BN 也能训练
3. **残差连接的帮助**：shortcut 连接有助于梯度传播，部分缓解了不稳定

**观察到的不稳定表现：**

1. **收敛速度变慢**：无 BN 模型需要更多 epoch 才能达到相近的 Loss
2. **初期准确率偏低**：第 1 个 epoch 时，无 BN 模型准确率仅 28-39%，而有 BN 模型达到 34-45%
3. **Warmup 组初期更慢**：前 5 个 epoch 使用小学习率，导致初期收敛明显滞后

### 5.3 Warmup 的改善效果

从实验数据来看，Warmup 的改善效果并不明显：

1. **最终性能略差**：Warmup 组最终准确率 84.67%，低于无 Warmup 的 85.25%
2. **Loss 波动更大**：Warmup 组标准差 0.4468，高于无 Warmup 的 0.3976
3. **收敛速度更慢**：Warmup 组最终 Loss 0.3549，高于无 Warmup 的 0.3428

**Warmup 改善了什么？**

Warmup 主要改善了训练初期的平滑性，从 Loss 曲线可以看到前 5 个 epoch 下降更平缓。但在本实验中，这种改善并未转化为最终性能提升。

**Warmup 为什么不能完全替代 BatchNorm？**

1. **作用机制不同**：Warmup 仅调整学习率调度，BN 直接规范化网络内部特征分布
2. **BN 的加速效果**：BN 不仅稳定训练，还能加速收敛，这是 Warmup 做不到的
3. **本实验学习率适中**：0.1 的学习率本身不算过大，Warmup 的"保护"作用有限

### 5.4 Loss 波动的定量分析

使用标准差衡量 Loss 波动程度：

| 模型 | Loss 标准差 | 相对波动 |
|------|-----------|----------|
| With BN | 0.3953 | 基准 |
| No BN | 0.3976 | +0.6% |
| No BN + Warmup | 0.4468 | +13.0% |

分析：
1. 三组 Loss 标准差差异不大，说明本实验中训练整体较为稳定
2. Warmup 组波动略大，可能是因为学习率变化引入的额外变量
3. 本实验中未出现剧烈波动，主要得益于适中的学习率和数据增强

### 5.5 最终性能对比

| 指标 | With BN | No BN | No BN + Warmup |
|------|---------|-------|----------------|
| 最终训练损失 | 0.2322 | 0.3428 | 0.3549 |
| 最终测试准确率 | 85.86% | 85.25% | 84.67% |
| 最高测试准确率 | 87.54% | 85.32% | 86.02% |

分析：
1. **With BN 最优**：训练损失最低，测试准确率最高
2. **No BN 次之**：最终性能略低于 With BN，但差距不大（0.6%）
3. **Warmup 无增益**：在本实验设置下，Warmup 未带来性能提升，反而略有下降

## 6 思考题

### 思考题 1：为什么 ResNet 已经有残差连接了，仍然需要 BatchNorm？

残差连接解决的是梯度传播问题，通过 shortcut 让梯度可以直接回传，缓解梯度消失。但残差连接并不能解决内部协变量偏移（Internal Covariate Shift）问题。

BatchNorm 的作用是：
- 稳定每一层的输入分布
- 减少层与层之间的相互依赖
- 允许使用更大的学习率

两者解决的是不同的问题，因此即使有残差连接，BatchNorm 仍然很重要。

### 思考题 2：如果把学习率调得很小，无 BatchNorm 的模型还能训练吗？代价是什么？

可以训练，但代价是：
- 收敛速度极慢
- 可能需要数倍的训练时间
- 最终性能可能仍不如使用 BatchNorm 的模型
- 对学习率的选择非常敏感

这说明 BatchNorm 不仅是稳定训练的工具，也是加速收敛的重要技术。

### 思考题 3：Warmup 是如何改善训练初期稳定性的？

训练初期，网络参数是随机初始化的，此时：
- 各层输出的尺度不稳定
- 梯度可能很大
- 使用大学习率容易导致参数更新过猛

Warmup 通过逐步增大学习率，让网络在初期以较小的步长探索，等参数逐渐稳定后再加速训练。这相当于给网络一个"热身"过程。

### 思考题 4：如果 batch size 很小，BatchNorm 会不会出现问题？为什么？

会出现问题。BatchNorm 依赖于 batch 内的统计量（均值和方差）：
- batch size 太小时，统计量估计不准确
- 导致归一化效果不稳定
- 可能引入额外的噪声

解决方案：
- 使用 GroupNorm 或 LayerNorm（不依赖 batch 维度）
- 增大 batch size
- 使用 Batch Renormalization

### 思考题 5：除了 BatchNorm 和 Warmup，还有哪些办法可以提高训练稳定性？

1. **其他归一化方法**：LayerNorm、GroupNorm、InstanceNorm
2. **学习率调度**：余弦退火、指数衰减、ReduceLROnPlateau
3. **梯度裁剪**：防止梯度爆炸
4. **权重初始化**：He 初始化、Xavier 初始化
5. **正则化技术**：Dropout、Weight Decay
6. **优化器选择**：Adam、AdamW（自适应学习率）
7. **混合精度训练**：使用 FP16 + Loss Scaling

## 7 实验总结

本次实验通过对比三个 ResNet-18 变体的训练过程，深入理解了 BatchNorm 和 Warmup 对训练稳定性的影响。

主要收获：

1. **BatchNorm 的重要性**：BatchNorm 能显著加速收敛，降低训练损失，并略微提升测试准确率。在本实验中，With BN 模型最终 Loss 为 0.23，远低于无 BN 的 0.34-0.35

2. **训练不稳定的识别**：本实验中未出现剧烈波动，说明学习率设置合理。不稳定通常表现为 Loss 大幅震荡、长时间不下降或准确率忽高忽低

3. **Warmup 的作用与局限**：Warmup 能平滑训练初期，但在本实验设置下未带来最终性能提升。Warmup 更适合学习率较大或模型对初始化敏感的场景

4. **深层网络训练的挑战**：即使没有剧烈不稳定，无 BN 模型的收敛速度仍明显较慢。BatchNorm 不仅是稳定工具，更是加速训练的关键技术

本次实验为理解深度学习训练技巧提供了直观的实践经验，对后续设计和调试深层网络具有重要指导意义。
