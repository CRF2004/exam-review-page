# 实验1：线性回归的从零实现 实验报告

- 课程：深度学习
- 实验名称：线性回归的从零实现
- 实验日期：2026年3月13日

## 1. 实验目的

1. 理解线性回归模型的基本思想，明确“特征加权求和 + 偏置”如何完成数值预测。
2. 掌握从数据构造、参数初始化、前向计算、损失计算、反向传播到参数更新的完整训练流程。
3. 在不使用 `torch.nn.Linear`、`torch.optim`、`nn.Module` 等高级封装的条件下，仅使用基础张量运算和自动求导完成模型训练。
4. 通过观察损失下降和参数收敛过程，加深对梯度下降算法物理意义的理解。

## 2. 实验原理

### 2.1 线性回归模型原理

线性回归假设输入特征与输出之间满足线性关系：

\[
\hat{y} = Xw + b
\]

其中，\(X\) 为输入特征矩阵，\(w\) 为权重向量，\(b\) 为偏置。模型训练的目标是找到一组参数 \(w,b\)，使预测值 \(\hat{y}\) 尽可能接近真实值 \(y\)。

### 2.2 均方误差损失函数

本实验采用均方误差（MSE）作为损失函数：

\[
L = \frac{1}{n} \sum_{i=1}^{n}(\hat{y}_i - y_i)^2
\]

MSE 对较大误差惩罚更强，适合回归任务。训练中通过最小化该损失来优化模型参数。

### 2.3 梯度下降算法基本思想

利用自动求导得到损失对参数的梯度后，按梯度反方向更新参数：

\[
w \leftarrow w - \eta \frac{\partial L}{\partial w}, \quad
b \leftarrow b - \eta \frac{\partial L}{\partial b}
\]

其中 \(\eta\) 为学习率。实验中采用小批量随机梯度下降（Mini-batch SGD），即每次用一个 batch 的样本计算梯度并更新参数。

## 3. 实验步骤

### 3.1 数据生成

使用 `torch.randn` 生成 1000 条样本、2 个特征的输入数据 `X`。设定真实参数：

- `true_w = [2.0, -3.4]`
- `true_b = 4.2`

按照 `y = X @ true_w + true_b + 噪声` 生成标签，并将 `y` reshape 为列向量，便于矩阵运算。

### 3.2 参数初始化

手动定义可训练参数：

- `w`：形状为 `(2,1)`，随机初始化，`requires_grad=True`
- `b`：标量偏置，初始化为 0，`requires_grad=True`

### 3.3 模型定义（前向计算）

定义线性回归函数：

- `linreg(X, w, b) = X @ w + b`

全部采用基础张量乘法与加法实现。

### 3.4 损失函数定义

定义 MSE：

- `mse_loss(y_hat, y) = ((y_hat - y) ** 2).mean()`

### 3.5 小批量 SGD 实现

手动实现参数更新函数 `sgd(params, lr)`，在 `torch.no_grad()` 环境下执行：

- `p -= lr * p.grad`
- `p.grad.zero_()`

同时实现 `data_iter(batch_size, X, y)` 用于打乱样本并按批次迭代。

### 3.6 训练循环

训练设置：

- `epochs = 5`
- `batch_size = 32`
- `lr = 0.03`

每个 epoch 内对所有 batch 执行“前向 -> 损失 -> backward -> 更新”。每个 epoch 结束后计算一次全数据集 loss 并输出，观察收敛趋势。

## 4. 实验结果

### 4.1 loss 变化

本次运行中，loss 变化如下：

| Epoch | Loss |
| --- | --- |
| 1 | 0.619408 |
| 2 | 0.013786 |
| 3 | 0.000391 |
| 4 | 0.000107 |
| 5 | 0.000100 |

可见损失快速下降并趋于稳定，说明模型训练有效。

### 4.2 参数对比

| 参数 | 真实值 | 训练后估计值 |
| --- | --- | --- |
| w1 | 2.0000 | 2.0000 |
| w2 | -3.4000 | -3.3995 |
| b | 4.2000 | 4.1997 |

训练得到参数与真实参数非常接近，表明从零实现流程正确。

## 5. 实验分析

### 5.1 学习率与 batch size 对训练的影响

1. 学习率过小：收敛慢，loss 下降速度明显变慢。
2. 学习率过大：容易震荡甚至发散，loss 可能上下波动。
3. batch size 较小：每步梯度噪声更大，但更新频繁，可能更快到达较优区域。
4. batch size 较大：梯度更稳定，但单个 epoch 更新次数减少，初期下降可能变慢。

本实验中 `lr=0.03, batch_size=32` 在稳定性和收敛速度之间取得了较好平衡。

### 5.2 梯度更新的物理意义

梯度表示损失函数在参数空间中的“最陡上升方向”。参数按负梯度方向更新，相当于沿着“下坡方向”移动，从而逐步降低损失。梯度绝对值越大，表示当前位置误差敏感度越高，参数更新幅度也应更大。

### 5.3 若初始权重全部设为 0 会怎样

线性回归中将 `w` 全部初始化为 0 仍可训练并收敛（与某些深层网络不同，不会出现严重对称性破坏问题）。但若初值离最优点较远，可能需要更多迭代才能收敛。随机初始化通常更有利于训练初期快速进入有效下降区间。

### 5.4 误差变大时梯度与更新如何变化

当预测误差 \((\hat{y}-y)\) 变大时，MSE 对参数的梯度通常会增大，因此参数更新步幅也会增大；这会推动模型更快纠正较大的预测偏差。若误差很小，梯度也会变小，更新逐步减弱，训练趋于稳定。

## 6. 实验总结

本实验完整实现了线性回归从零训练流程，重点收获如下：

1. 明确了“模型定义 + 损失函数 + 梯度下降”是监督学习训练的核心结构。
2. 在不依赖高级 API 的情况下，掌握了参数初始化、前向计算、反向传播、手动更新参数的关键细节。
3. 通过 loss 曲线与参数对比验证了实现正确性，并直观理解了梯度下降的优化机制。
4. 通过学习率与 batch size 的讨论，认识到超参数对收敛速度与稳定性有直接影响。

整体来看，本实验为后续更复杂神经网络（多层感知机、卷积网络等）的训练与调参打下了基础。
